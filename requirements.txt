# Unfortunately it seems that newer versions of networkx are causing problems
networkx == 2.5
matplotlib
numpy
scipy
argparse
configparser
pandas
# For jax
#optax
# For pytorch, the torch package should be installed, but I don't include it
#	here because it should be installed carefully so as to interface 
#	properly with the GPU.
